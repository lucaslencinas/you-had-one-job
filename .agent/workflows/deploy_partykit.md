---
description: Deploy the PartyKit server
---
This workflow deploys the PartyKit server located in `apps/party`.

1. Navigate to the party app directory
// turbo
2. Run the PartyKit deployment command
   ```bash
   cd apps/party && npm run deploy
   ```
   If this is your first time, the CLI will ask you to login via the browser. Follow the prompts.

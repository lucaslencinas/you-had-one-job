---
description: Deploy the web application to Vercel
---
This workflow deploys the Next.js web application located in `apps/web` to Vercel.

1. Navigate to the web app directory
// turbo
2. Run the Vercel deployment command
   ```bash
   cd apps/web && vercel
   ```
   Follow the interactive prompts to set up the project if this is the first time.
